import { CircularProgress } from "@mui/material";

/**
 * LoadingSpinner is a functional component that renders a circular loading spinner.
 * Useful for indicating loading states in the UI.
 *
 * @component
 * @returns {JSX.Element} A Material-UI CircularProgress spinner.
 */
export const LoadingSpinner = () => {
  return <CircularProgress />;
};
